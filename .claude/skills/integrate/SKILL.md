---
name: integrate
description: Run `make ci` with a commit message, and keep fixing issues until it passes.
---

Run `make ci MSG="<commit message>"`, picking a sensible commit message from the current changes. Keep fixing whatever it complains about and rerunning until it passes.
