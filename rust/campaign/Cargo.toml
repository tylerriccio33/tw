[package]
name = "campaign"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
godot = { version = "0.5.4", features = ["api-4-7"] }
rand = "0.8"

[dev-dependencies]
rand = { version = "0.8", features = ["small_rng"] }
